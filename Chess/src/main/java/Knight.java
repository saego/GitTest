/**
 Created by ${Ruslan} on 22.12.16.
 */
public class Knight extends Figure{
    public Knight(String colour, Cell cell) {
        super(colour, cell);
    }

    @Override
    public Cell[] way(Cell newPosition) throws ImpossibleToMoveException {
        boolean invalid = true;
        if (){
            invalid = false;
        }
        return new Cell[0];
    }
}
