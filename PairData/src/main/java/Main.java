import java.io.*;

/**
 Created by ${Ruslan} on 28.12.16.
 */
public class Main {
    public static void main(String []args) throws IOException {
        //ByteArrayInputStream testStream = new ByteArrayInputStream("1211641".getBytes());
        //InputStream testStream = new InputStream().read(System.in);

        Stream stream = new Stream();
        stream.isNumber(testStream);
    }
}
